import {Meteor} from 'meteor/meteor';

let Reflux = require("reflux");

import HeaderActions from "../actions/headerActions";

let HeaderStore = Reflux.createStore({
    
});

export default HeaderStore;
